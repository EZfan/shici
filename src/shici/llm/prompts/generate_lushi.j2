{% extends "system.j2" %}

{% block extra_rules %}
## 当前任务:创作一首律诗

- 体裁:**{{ form }}**(8 句、{% if form == "五言律诗" %}每句 **5** 字{% else %}每句 **7** 字{% endif %})
- 题材:『{{ theme }}』
{%- if title %}
- 拟定题目:**{{ title }}**
{%- else %}
- 请拟定题目,可自取亦可不题
{%- endif %}
{%- if rhyme_group %}
- 韵部:押 **{{ rhyme_group }}** 韵部(全诗八句中第二、四、六、八句末字必须为韵脚;首句可入韵可不入韵)
{%- else %}
- 韵部:第二、四、六、八句末字必须押同韵部,首句可入韵
{%- endif %}
{%- if keywords %}
- 关键词(化入句中):{{ keywords | join("、") }}
{%- endif %}
{%- if reference %}
- 参考风格:『{{ reference }}』
{%- endif %}

## 严格要求

1. 共 **8** 句,{% if form == "五言律诗" %}每句恰 **5** 字{% else %}每句恰 **7** 字{% endif %}。
2. **颔联**(**第 3、4 句**)与**颈联**(**第 5、6 句**)必须**对仗工整**:
   - 字数相等(显然)
   - 词性相对(名词对名词,动词对动词,形容词对形容词,虚词对虚词)
   - 句法结构相对
   - 上下句语义相关、相对或相反
3. 平仄严格依照常用正格。
4. **首联、尾联**不必对仗,但可作为情感铺垫与收束。
5. 韵脚位置(第二、四、六、八句及可能的入韵首句)末尾字须同属一韵部,填入 ``rhyme_group``;非韵脚行填 ``null``。
6. ``tone_pattern`` 长度 = 每行字数,逐字标注。
7. ``theme`` 用一句话概括主旨,不超过 40 字。
8. ``notes`` 可简要说明对仗取义、章法布局,不超过 200 字。

## 输出 JSON Schema

```json
{
  "form": "五言律诗 | 七言律诗",
  "title": "string | null",
  "lines": [
    {
      "text": "string",
      "tone_pattern": ["平" | "仄" | "中", ...],
      "rhyme_group": "上平一东 | ... | null"
    }
  ],
  "theme": "string",
  "notes": "string | null"
}
```
{% endblock %}
