{% extends "system.j2" %}

{% block extra_rules %}
## 当前任务:为给出的上联撰写下联(必要时另拟上联)

### 任务 A — 已有上联,撰写下联

上联:
> {{ upper_line }}

要求:

1. **字数一致**:下联与上联字数严格相同(若上联含标点,下联可保留对等标点,但字数不计标点)。
2. **平仄相对**:每一字的平仄与上联**相反**(上平对下仄、上仄对下平),允许个别"中"字通融。
3. **词性相对**:
   - 名词对名词(且语义相近或相对,如"天"对"地"、"山"对"水")
   - 动词对动词
   - 形容词对形容词
   - 副词对副词
   - 虚词对虚词
4. **结构相对**:主谓、动宾、偏正、并列等结构相应。
5. **末字韵部**:下联末字须入韵,填入 ``rhyme_group``。
6. **意境相关或相对**为佳,不必字字对仗,可有适当宽对,但**核心词(本联灵魂)**必须精准对应。
7. 若上联尾字为平声,下联尾字可为平亦可为仄;常规做法下联末字仄声收束,需根据上联具体情形灵活处理。

### 任务 B — 无上联,创作完整对联(可选)

{%- if not upper_line %}
未给出上联,请自拟上下联:
- 题材:『{{ theme }}』
- 字数:**{{ length }}** 字对
- 表达场景:{{ occasion | default("通用") }}
- 既要有上联(``upper_line`` 字段),也要给出对应的下联。
{%- endif %}

## 输出 JSON Schema

注意:**GeneratedPoem** 仍用于表示对联,``lines`` 仅含两行——上联``lines[0]``,下联``lines[1]``。

```json
{
  "form": "对联",
  "title": "string | null",
  "lines": [
    {
      "text": "上联文本",
      "tone_pattern": ["平" | "仄" | "中", ...],
      "rhyme_group": "上平一东 | ... | null"
    },
    {
      "text": "下联文本",
      "tone_pattern": ["平" | "仄" | "中", ...],
      "rhyme_group": "上平一东 | ... | null"
    }
  ],
  "theme": "string",
  "notes": "string | null"
}
```
{% endblock %}
