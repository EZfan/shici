{% extends "system.j2" %}

{% block extra_rules %}
## 当前任务:创作一首绝句

- 体裁:**{{ form }}**(4 句、{% if form == "五言绝句" %}每句 **5** 字{% else %}每句 **7** 字{% endif %})
- 题材:『{{ theme }}』
{%- if title %}
- 拟定题目:**{{ title }}**(可保留,亦可改易)
{%- else %}
- 不必拟定题目;若题材极为凝练可自拟,但须注明
{%- endif %}
{%- if rhyme_group %}
- 韵部:押 **{{ rhyme_group }}** 韵部
{%- else %}
- 韵部:自由选择常用韵部,但请在同一作品内统一
{%- endif %}
{%- if keywords %}
- 关键词(尽量化入):{{ keywords | join("、") }}
{%- endif %}
{%- if reference %}
- 参考风格或意境:『{{ reference }}』
{%- endif %}

## 严格要求

1. 共 **4** 句,{% if form == "五言绝句" %}每句恰 **5** 字{% else %}每句恰 **7** 字{% endif %},不含标点。
2. 平仄严格依照常用正格(首句入韵用平起 / 仄起任选其一,但全篇须自洽)。
3. **第二、三句**(颔联)**对仗**(词性相对、句法结构相对),**不必对仗**——绝句对仗仅是常见做法,非强制;若欲出奇制胜可不对仗,但须在意境上有过人之姿。
4. **末句点题**,留有余味。
5. 每行末尾若为韵脚,填写对应的 ``rhyme_group``;否则填 ``null``。
6. ``tone_pattern`` 长度 = 每行字数,逐字标注。
7. ``theme`` 字段用一句话点明创作意图,不超过 40 字。
8. ``notes`` 字段为可选的创作札记(化用典故、章法安排),不超过 120 字。

## 输出 JSON Schema

```json
{
  "form": "五言绝句 | 七言绝句",
  "title": "string | null",
  "lines": [
    {
      "text": "string",
      "tone_pattern": ["平" | "仄" | "中", ...],
      "rhyme_group": "上平一东 | ... | null"
    }
  ],
  "theme": "string",
  "notes": "string | null"
}
```
{% endblock %}
